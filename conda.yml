name: tfm-apple-sentiment

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - scikit-learn==1.8.0
      - joblib
      - numpy
      - nltk
      - azureml-inference-server-http